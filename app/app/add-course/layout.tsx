export const metadata = {
  title: 'Add Course',
  description:
    'Create and manage your courses with ease. Add new courses, organize content, and track progress all in one place.',
};

const CourseLayout: React.FC<{ children: React.ReactNode }> = ({ children }) => {
  return <>{children}</>;
};

export default CourseLayout;
